

class {{ artifacts.required.name }}({{ artifacts.required.parent_class }}, total=True):
    """{{ artifacts.model_name }} TypedDict for properties that are required."""
{% for prop in artifacts.required.props %}
    {{ prop.name }}: {{ prop.type }}{% endfor %}