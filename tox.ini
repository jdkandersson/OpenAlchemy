# content of: tox.ini , put in same dir as setup.py
[tox]
envlist = py36,py37,py38

[testenv]
deps =
    pytest
    pytest-cov
    pytest-flakes
    typing-extensions
    sqlalchemy
commands =
    pytest
